// Define minimum and maximum team size, used during validation of data.
export const MAX_TEAM_SIZE = 5;
export const MIN_TEAM_SIZE = 1;

// Maximum no of tries for generating team code
export const MAX_TEAM_CODE_GEN_TRIES = 5;
